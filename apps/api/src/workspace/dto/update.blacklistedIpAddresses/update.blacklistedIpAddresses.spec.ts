import { UpdateBlacklistedIpAddresses } from './update.blacklistedIpAddresses'

describe('UpdateBlacklistedIpAddresses', () => {
  it('should be defined', () => {
    expect(new UpdateBlacklistedIpAddresses()).toBeDefined()
  })
})
